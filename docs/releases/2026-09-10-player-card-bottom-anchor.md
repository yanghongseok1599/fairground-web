# 선수카드 상반신 하단 고정 — 2026-09-10

## 수정

- 기본 사진 크기 92%에서 상반신이 카드 구분선 위로 떠 있던 문제를 수정했다.
- 원인은 상반신 크롭의 확대·축소 기준이 `center top`이었던 것이다. `center bottom`으로 바꿔 잘린 몸통의 하단이 사진 슬롯 하단에 고정되도록 했다.
- 사진 슬롯 좌표, 얼굴 합성, 상반신 72% 크롭, 저장된 원본 사진, 이름·스탯 위치는 변경하지 않았다. 사진 재등록 없이 기존 카드에 적용된다.
- 공통 표시 스타일 한 모듈만 변경하므로 선수카드와 저장용 캡처에 함께 적용된다. 사진 등록 미리보기는 배율 1로 표시되어 기존 구도를 유지한다.

## 검증

- 남녀 준타스 가상 선수 예시를 실제 컴포넌트로 렌더링했다. 기본 0.92 배율에서 프레임과 크롭 하단의 DOM 좌표 차이가 각각 0px이었다.
- html2canvas 저장용 캡처에서도 상반신 하단이 카드 구분선에 붙는 것을 확인했다.
- 0.5 / 0.92 / 1 / 1.5 / 2.5 배율 및 5개 높이의 하단 고정 회귀 테스트와 기존 카드 테스트 4종 통과.
- 메인 TypeScript·프로덕션 빌드 통과. 메인 및 분리 배포 소스 린트 오류 0, 기존 경고 18개 유지. 분리 소스 TypeScript 통과.
- Lazyweb 선수 프로필 참고를 확인했으며, 정확한 기준선은 사용자가 제공한 현재 카드 구분선을 그대로 사용했다.

## 배포 안전 범위

- 운영 기준 `dpl_4skGnUyZCTxuXYfdjQa9czKpNpwc`의 881개 파일과 분리 소스가 수정 전에 일치했다. 이번 변경은 공통 스타일·회귀 테스트뿐이며 빌드 타입 캐시 외의 다른 기존 기능 소스는 변경하지 않았다.
- Supabase 개발/운영 분리 및 원격 마이그레이션 목록을 읽기 전용으로 확인했다. 기존 이력 불일치는 그대로이며 DB·마이그레이션·회원 사진을 수정하지 않았다.
- DB 참조는 직전 배포와 동일하다. 직전 배포에서 조회한 운영 카탈로그로 분리 소스의 함수 35개·릴레이션 27개·버킷 1개를 대조해 누락 0개를 확인했다. 이번에는 운영 카탈로그를 새로 조회하지 않았다.
- Vercel `SUPABASE_DB_URL` 미설정으로 이번 배포 명령에만 `FAIRGROUND_SKIP_DB_PARITY=1`을 사용했다. 영구 환경변수 설정은 바꾸지 않았다. 메인의 미배포 `register_team` 변경은 포함하지 않았다.
- 실제 회원 로그인·저장 및 실물 iPhone Safari는 이번 검증 범위에 포함하지 않았다.

운영 배포 `dpl_7XET3FDMvLtNgwG9VVZdNJHG3s4e` READY 및 `fairground-kor.com` 연결 완료. Vercel 프로덕션 빌드도 통과했다.
