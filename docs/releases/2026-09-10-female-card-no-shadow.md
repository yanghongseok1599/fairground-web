# 여자 선수 인물 그림자 제거 — 2026-09-10

- `PlayerCard`에서 여자 선수의 `UpperBodyPortrait`에 그림자 효과를 적용하지 않도록 했다. 남자 선수의 기존 효과와 카드 테두리 그림자는 변경하지 않았다.
- 얼굴·머리카락·유니폼 픽셀, 상반신 크롭, 하단 정렬, 사진 원본은 변경하지 않는다. 기존 사진도 재등록 없이 적용되며 같은 컴포넌트를 사용하는 카드 캡처에도 반영된다.
- 브라우저에서 남녀 예시를 확인했다. 여자 인물의 computed `filter`는 `none`, 남자는 기존 `drop-shadow`이며, 양쪽 하단 간격은 0px이다.
- 메인 린트 오류 0(기존 경고 18), TypeScript, 상반신 회귀 테스트, 기존 카드 테스트 4종, 프로덕션 빌드 통과. 분리 배포 소스 린트·TypeScript·Supabase 안전장치 4개 통과.
- Lazyweb 배경 제거 참고를 확인했지만 사진을 다시 만들거나 형태를 바꾸지 않고, 요청한 인물 그림자 효과만 제거했다.
- 운영 `dpl_7XET3FDMvLtNgwG9VVZdNJHG3s4e`와 분리 배포 소스를 비교해 변경이 `src/components/player-card.tsx` 한 파일뿐임을 확인했다(나머지 880개 일치, 누락 0). 이후 생성되는 타입 캐시 외의 관련 없는 기능 변경은 포함하지 않는다.
- Supabase 개발/운영 분리와 원격 이력은 읽기 전용으로 확인했다. DB·마이그레이션·회원 사진을 수정하지 않는다. 기존 마이그레이션 이력 불일치는 남아 있다.
- DB 참조 변경 없음. 앞선 배포에서 조회한 운영 카탈로그로 함수 35개·릴레이션 27개·버킷 1개 누락 0을 재확인했다. 이번에는 운영 카탈로그를 새로 조회하지 않았다.
- Vercel `SUPABASE_DB_URL` 미설정 때문에 배포 명령 한 번에만 `FAIRGROUND_SKIP_DB_PARITY=1`을 사용하며 영구 환경변수는 바꾸지 않는다. 미배포 `register_team` 변경도 제외했다.
- 실제 회원 로그인/저장, 실물 iPhone Safari, 원본 사진에 이미 들어 있는 촬영 그림자의 자동 제거는 이번 범위에 포함하지 않는다.

운영 배포 `dpl_HNnfpgSAFHDCEvNQfxiaJs1bLX9d` READY 및 `fairground-kor.com` 연결 완료. Vercel 프로덕션 빌드도 통과했다.
